query getHero($episode: Episode!) {
  hero(episode: $episode) {
    id
    name
    friends {
      edges {
        node {
          id
          name
        }
      }
      totalCount
    }
  }
}

 query GetCharacters($ids: [Int!]!){
  character(ids: $ids) {
    id
    name
  }
}